function slove(character) {
  if (character >= "A" && character <= "Z") {
    console.log(`upper-case`);
  } else if (character >= "a" && character <= "z") {
    console.log(`lower-case`);
  }
}

slove("L");
